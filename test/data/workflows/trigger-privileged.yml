name: Trigger Privileged
on: [pull_request_target]
jobs:
  deploy:
    permissions:
      contents: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "High-risk + privileged"