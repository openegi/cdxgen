name: Checkout Credentials Test
on: [push]
jobs:
  deploy:
    permissions: { contents: write }
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # Should trigger CI-005 (default persist-credentials=true)