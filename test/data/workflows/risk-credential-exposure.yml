name: Risk Credential Exposure
on: [push]
jobs:
  deploy:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # persist-credentials=true (default)
      - run: git push