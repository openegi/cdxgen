name: Cache Array Restore
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v4
        with:
          path: ~/.cargo
          key: cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys:
            - cargo-
            - cargo-abc
            - cargo-def