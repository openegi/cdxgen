name: Cache Poisoning Test
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4  # Should trigger CI-006
        with:
          path: ~/.npm
          key: npm-${{ hashFiles('**/package-lock.json') }}